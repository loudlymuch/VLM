# =============================================================================
# VLM QLoRA 微调项目依赖
# =============================================================================
# 安装方式: pip install -r requirements.txt
# 建议在 conda 环境中安装，Python >= 3.10
#
# 注意：torch 建议根据你的 CUDA 版本从 https://pytorch.org 安装，
#       以下列出的 torch 为参考版本（CUDA 12.1）。
# =============================================================================

# ---------------------------------------------------------------------------
# 深度学习框架
# ---------------------------------------------------------------------------
torch>=2.1.0                      # PyTorch 核心（QLoRA 需要 >=2.1）
# torchvision                      # 如需图像预处理增强可安装

# ---------------------------------------------------------------------------
# HuggingFace 生态
# ---------------------------------------------------------------------------
transformers>=4.45.0               # Qwen2.5-VL 模型加载、QLoRA 训练（AutoProcessor, Trainer 等）
datasets>=2.18.0                   # 数据集下载、加载、处理（load_dataset, concatenate_datasets）
peft>=0.10.0                       # QLoRA / LoRA（LoraConfig, PeftModel, prepare_model_for_kbit_training）
bitsandbytes>=0.43.0              # 4-bit NF4 量化 + 双重量化（BitsAndBytesConfig 后端）
accelerate>=0.28.0                # 分布式训练 & 混合精度训练（Trainer + 量化的底层依赖）

# ---------------------------------------------------------------------------
# Qwen 视觉语言模型工具
# ---------------------------------------------------------------------------
qwen-vl-utils                      # Qwen2.5-VL 视觉信息处理（process_vision_info）

# ---------------------------------------------------------------------------
# 图像处理
# ---------------------------------------------------------------------------
Pillow>=10.0.0                     # 图像加载与保存（PIL.Image）

# ---------------------------------------------------------------------------
# 评估指标
# ---------------------------------------------------------------------------
numpy>=1.24.0                      # 数值计算（训练 loss 聚合等）
rouge-score>=0.1.2                 # ROUGE-L 文本相似度评估（eval.py）
nltk>=3.8.0                        # BLEU 评估（sentence_bleu, SmoothingFunction）

# ---------------------------------------------------------------------------
# Web 演示
# ---------------------------------------------------------------------------
gradio>=4.0.0                      # Web UI 交互式推理演示（src/web.py）
